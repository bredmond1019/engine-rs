---
type: Index
title: crates/engine-core/planning Index
description: Index for the crate-local planning root at crates/engine-core/planning — a stray, non-canonical SDLC planning location distinct from the repo's canonical planning/ (symlinked to core/_planning/engine-rs/).
doc_id: planning-index
layer: [engine]
project: engine-rs
status: active
keywords: [index, engine-core, crate-local-planning]
related: [knowledge, memory, archive-index]
---

# Index — crates/engine-core/planning

This is **not** the canonical engine-rs planning root — that is the repo-root `planning/` symlink
into `core/_planning/engine-rs/`. This crate-local directory holds artifacts from three SDLC blocks
that ran with `crates/engine-core` as their cwd/planning-root; see `memory.md` for the full story
and a guardrail candidate to prevent recurrence.

| File/Folder | What it is |
|---|---|
| `knowledge.md` | Distilled durable knowledge specific to the blocks executed here |
| `memory.md` | Episodic notes, gotchas, and the origin story of this stray planning root |
| `archive/` | Retired block folders (see `archive/index.md`) |

## Active candidates

None — all three blocks that used this planning root (`EN.5.D-policy-dispatch-seam`,
`EN.5.F-async-run-lifecycle`, `EN.6.A-egress-dispatch`) are archived. See `archive/index.md`.
