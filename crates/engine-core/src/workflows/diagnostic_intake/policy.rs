//! `DiagnosticIntakePolicy` / `PartialDiagnosticIntakePolicy` and the
//! `Policy` trait delegation to `crate::policy::resolve::resolve`. Filled by
//! task 2 (`planning/EN.4.B-diagnostic-intake/tasks.json`).
