//! `IntakeExtractNode` — the single, terminal, policy-aware structured
//! extraction node (pure extraction; no `WebSearch`/`WebFetch`). Filled by
//! task 5 (`planning/EN.4.B-diagnostic-intake/tasks.json`).

/// Placeholder for the single terminal model node. Re-exported from
/// `mod.rs` for EN.4.C to import by name; task 5 fills in the real
/// `ClaudeCodeStep`-backed implementation.
pub struct IntakeExtractNode;
