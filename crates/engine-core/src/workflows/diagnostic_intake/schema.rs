//! `DiagnosticIntakeEventSchema` (the triggering event `data` shape) and the
//! `DiagnosticIntake` structured output type. Filled by task 3
//! (`planning/EN.4.B-diagnostic-intake/tasks.json`).

/// Placeholder for the `DiagnosticIntake` structured output type (the
/// evidence contract from `intake.md §3`). Re-exported from `mod.rs` for
/// EN.4.C to import by name; task 3 fills in the real shape.
pub struct DiagnosticIntake;
