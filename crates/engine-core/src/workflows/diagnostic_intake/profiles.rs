//! Named `PartialDiagnosticIntakePolicy` bundles, the
//! `diagnostic_intake.{policy,profiles}` `harness.json` section, and
//! `resolve_policy_for_run`. Filled by task 4
//! (`planning/EN.4.B-diagnostic-intake/tasks.json`).
