//! `PersistToBrainNode` — the terminal `HttpPost` write of a
//! `LearningArtifact` to Synapse `POST /ingest/*` (EN.5.A task 11 scaffold;
//! D51: no embedding or pgvector in engine-rs).
//!
//! Filled in task 11 — see `planning/EN.5.A-content-pipeline/tasks.json`.
