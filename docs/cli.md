---
type: Reference
title: engine-rs CLI
description: Synopsis, subcommands, global flags, exit codes, and examples for engine-rs.
doc_id: cli
layer: [engine, console]
project: engine-rs
status: active
keywords: [CLI, subcommands, exit codes, Rust, workflow runtime]
related: [docs-index, context, master-plan]
---

# engine-rs — CLI

> `engine-rs` has no standalone CLI at project start — it is a library/runtime that embeds in the
> `bastion serve` daemon (see `docs/architecture.md`). This doc is scaffolded now and filled in as
> Phase 0/1 blocks land; if a thin `engine-rs` binary (e.g. for local dry-runs or fixture
> round-trip checks) is added, its surface is documented here.

## Synopsis

<!-- Stub — e.g. `bastion serve` embeds engine-rs; no separate binary yet. -->

## Subcommands

<!-- Stub — populate from `src/main.rs` once a binary target exists. -->

| Subcommand | Description |
|---|---|
| (none yet) | |

## Global Flags

<!-- Stub. -->

## Exit Codes

<!-- Stub. -->

## Examples

<!-- Stub. -->
